# ============================================================
# TradingCalculator.pro — Dependencias Python
# Compatible con Python 3.11 + Google Cloud Run
# ============================================================

# Framework principal
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Base de datos (Cloud SQL PostgreSQL)
asyncpg==0.30.0
sqlalchemy[asyncio]==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.0

# Seguridad / Auth
PyJWT==2.9.0
bcrypt==4.2.1
python-multipart==0.0.12
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
passlib[bcrypt]==1.7.4
cryptography>=41.0.0

# Pagos
stripe==11.3.0

# IA (Claude trade coach)
anthropic>=0.40.0

# Rate limiting
slowapi==0.1.9

# Email
sendgrid==6.11.0

# HTTP / utilidades
httpx==0.28.1
aiohttp==3.11.10
python-dotenv==1.0.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Cálculos financieros + datos de mercado
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
yfinance==0.2.54
curl_cffi>=0.6.0          # browser impersonation para que Yahoo no bloquee las IPs de Cloud Run

# Google Cloud (Cloud SQL connector, Secret Manager)
cloud-sql-python-connector[asyncpg]==1.12.1
google-cloud-secret-manager==2.22.0
google-cloud-logging==3.11.3

# CORS y middleware
starlette==0.41.3
